# 글 발행 워크플로우 (Google 전용)

> 한국 Google + AI 검색엔진 타겟. 네이버 무시.

---

## 🎯 한 줄 요약

> "강아지 영양제 글 써줘" → Claude가 아래 5단계 자동 실행

---

## 핵심 차별점: Google vs 네이버

Google.co.kr 검색자는 **네이버 사용자와 다른 의도**를 가지고 있어요.

| 톤 | 네이버형 | **Google형 (우리 타겟)** |
|---|---|---|
| 후기 검증 | "사용 후기" 감성 | **"디시 후기 종합"** + **"논란 정리"** |
| 비교 | "OO 추천" | **"OO 순위 / 등급표 / 티어"** |
| 신뢰 | 인플루언서 | **광고 의심 / oem / 부작용 검증** |
| 가격 | "가격 알려주세요" | **"비용 비교 / 가성비 vs 프리미엄"** |
| 구조 | 정성적 서술 | **데이터 테이블 + 객관 비교** |

---

## 🚨 운영 위반 절대 금지 (Claude 세션마다 필독)

### 본문 작성 시
- ❌ "직접 써본 결과" 같은 **허위 사용 후기** 작성 금지 (실제 사용 X)
- ❌ "100% 효과", "절대 안전" 같은 **과장 표현** 금지 (식약처 위반)
- ❌ 의약품 효능 표기 금지 (사료/간식에 "치료", "예방" 표현 X)
- ❌ "쿠팡이 추천한", "쿠팡 공식 1위" 같은 **쿠팡 사칭** 금지
- ❌ 쿠팡 로고 이미지 직접 삽입 금지 (지식재산권)
- ✅ "데이터에 따르면", "성분 분석 결과" 같은 **객관적 표현** 사용
- ✅ 출처 인용 시 외부 권위 (AAFCO, VCA, 학술자료) 우선

### 운영 시
- ❌ **본인 클릭 절대 금지** (가족·친구 포함, 동일 가구 IP 잡힘)
- ❌ "쿠팡 사료" 같은 **쿠팡 키워드로 광고** X (구글 애드워즈 등)
- ❌ 커뮤니티에 파트너스 링크 직접 살포 금지
- ❌ 가격 표기 시 "최저가 보장" 등 단정적 표현 X (실시간 가격 변동)

### 처벌 수준 (참고)
- 1회 위반: 14일 수익 몰수
- 2회 위반: 30일 수익 몰수 + **계정 영구 정지**
- 공정위 가이드라인 위반: 수천만원대 소급 몰수 사례 존재

---

## 5단계 워크플로우

### 1️⃣ 데이터 수집 + 의도 분류 + 스켈레톤 생성

```bash
cd "/Users/kimjaehyeong/Desktop/부업마스터/파트너스 수익/content-pipeline"
node new-article.js "강아지 영양제" "건강"
```

자동 발생:
- **Google.co.kr 자동완성 + 확장 (디시/더쿠/논란/순위 prefix)**
- 의도별 자동 분류 (comparison / review / trust / guide / price / diy)
- 쿠팡 상품 3개 + 파트너스 딥링크
- 마크다운 스켈레톤 생성

> ⚠️ **슬러그(파일명·URL)는 반드시 영어 kebab-case로 작성**
>
> - ✅ 좋은 예: `dog-joint-supplement-2026`, `dog-allergy-food-recommendation-2026`
> - ❌ 나쁜 예: `강아지-관절-영양제-2026` (한글 URL은 인코딩 시 깨지고 GSC·로그에서 가독성 떨어짐)
> - `new-article.js`가 한글 슬러그를 생성하면 **즉시 영어로 rename** 후 frontmatter·이미지 파일명까지 일치시킬 것
> - 메인키워드·제목·본문은 그대로 한국어 유지. **슬러그만 영어**다.

### 2️⃣ Google 톤으로 본문 작성

스켈레톤의 `TODO`를 다음 규칙대로 채우기.

#### 제목 패턴 (Google CTR 최적화)

| 의도 | 패턴 | 예시 |
|---|---|---|
| comparison | "{키워드} 순위 + 등급표 (2026)" | "강아지 영양제 순위 + 성분 등급표 (2026)" |
| review | "{키워드} 디시 후기 종합" | "강아지 영양제 디시·더쿠 후기 종합 분석" |
| trust | "{키워드} 논란/부작용 검증" | "강아지 영양제 oem 광고 논란 검증" |
| guide | "{키워드} 시기/방법 가이드" | "강아지 영양제 언제부터 - 연령별 가이드" |

#### 메타 정보
- `title`: 60자 이내, 메인 키워드 + 검색 의도 키워드(순위/논란/디시 등) 포함
- `description`: 150자 이내, **"객관·비교·검증" 톤** 강조
- `tldr`: **데이터 기반 결론** (감성 X)
- `faq`: 4개 이상, **"왜 이 제품?" 질문 의무 포함**

#### 본문 구조 (Wirecutter 스타일)

1. **첫 H2: "{메인키워드}, 한 줄로 정리하면?"**
   - 첫 문단 100자 내 결론
   - 객관적 평가어 ("X로 판단됨", "데이터에 따르면") 사용
   - 감성어("따뜻한", "사랑스러운") 금지

2. **두 번째 H2: 평가 기준 / 등급 체계**
   - 표 1개 (어떤 기준으로 평가하는지)
   - 인포그래픽 1개

3. **세 번째 H2: 추천 제품 비교 (등급표)**
   - 비교 표 (필수: 가격·성분·등급)
   - 상품별 H3 + `{{COUPANG_LINK_N}}`
   - 각 상품에 **"왜 이 등급인지"** 데이터 근거 명시

4. **네 번째 H2: 디시/더쿠 후기 종합** (review intent 시)
   - 커뮤니티 의견 4~5건 인용 (가짜 X, 일반적 패턴 정리)
   - 긍정/부정 균형

5. **다섯 번째 H2: 논란 / 검증** (trust intent 시)
   - oem·광고·부작용 의심 사례
   - 어떻게 회피하는지 가이드
   - 외부 권위 출처 인용 (FDA, AAFCO, 학술자료 등)

6. **여섯 번째 H2: FAQ**
   - "왜 X가 Y보다 좋은가?" 형태 4개 이상
   - 답변에 **수치·근거** 포함

#### SEO 체크리스트
- [ ] 메인 키워드 → 제목 / H1 / 첫 문단 100자 / 메타 / alt
- [ ] **검색 의도 키워드 1개 이상 제목 포함** (디시/순위/논란/등급)
- [ ] LSI 키워드 5개 이상 본문 자연 삽입
- [ ] 표 최소 2개 (평가표 + 비교표)
- [ ] FAQ 4개 이상
- [ ] 외부 권위 출처 1~2개
- [ ] 내부 링크 자리표시자 2~3개 — **반드시 `/slug/` 형식 (끝에 슬래시 필수)**. `/slug`(슬래시 없음)·`/posts/slug` 금지
- [ ] 본문 1500자 이상
- [ ] **광고/논란 키워드를 만나면 "투명성 박스"** 추가 (어필리에이트 고지 명시)

### 3️⃣ 인포그래픽 생성 (텍스트 없이)

> ⚙️ **이미지 엔진 = Replicate Flux Schnell 전용** (장당 ~$0.003). `.env`에 `IMAGE_PROVIDER=replicate`. **이미지 생성 코드 임의 수정 금지.**
>
> 🚫 **Gemini 은퇴 (2026-06-29~, 사용자 지시 "앞으로 flux로 전부")**: `generate-images.js`/`publish-batch.js`의 자동 프롬프트 합성(`synthesizePrompts`)은 gemini-2.5-flash라 크레딧 소진(429 depleted) 시 막힌다. **이제 Gemini 자동 합성에 의존하지 않는다.** 대신 **Claude가 시각 묘사 2종을 직접 영문으로 작성해 Flux로 바로 생성**한다(아래 표준 명령). 즉 `generate-images.js <slug>`(내부 Gemini 합성 경로)는 쓰지 말 것.

**표준 이미지 생성 (Claude 직접 작성 → Flux)**:
```bash
# Claude가 글 frontmatter(mainKeyword·products·title)를 보고 영문 시각 묘사 2종 작성:
#   cover = 단일 제품 실루엣 커버 컷(썸네일 -1), compare = 3제품 비교 일러스트(본문 -2)
node -e '
const { generateForArticle } = require("./generate-images.js");
const cover = "<단일 제품 커버 영문 묘사>";
const compare = "<3제품 비교 영문 묘사>";
generateForArticle("<slug>", [cover, compare]).then(()=>console.log("DONE"));
'
# 그다음 발행만 (이미지 재생성 금지):
node publish-batch.js --images 0 <slug>
```

> 참고: `generateForArticle`가 내부에서 `buildImagePrompt`(브랜드 팔레트·무텍스트 규칙)를 자동 적용하므로 묘사는 장면만 쓰면 된다. rsvg 로컬 SVG 폴백은 Replicate까지 불가일 때만 최후로.

> 🚫🚫 **Gemini 절대 금지 (2026-07-28 사용자 재확인 — "이미지 생성 gemini 절대 쓰지마")**
> 이미지 파이프라인 어디에도 Gemini를 쓰지 않는다. 아래는 **실행 금지 명령**이다.
>
> ```bash
> node generate-images.js [slug] 2      # ❌ 금지 — 내부에서 gemini-2.5-flash로 시각 묘사 합성
> node publish-batch.js [slug]          # ❌ 금지(--images 미지정) — 같은 Gemini 합성 경로 탐
> ```
>
> - **시각 묘사는 항상 Claude가 직접 영문으로 작성**한다. `synthesizePrompts()`(Gemini)는 호출하지 않는다.
> - 이미지 생성은 **Replicate Flux Schnell 전용**(`IMAGE_PROVIDER=replicate`).
> - 발행은 반드시 **`node publish-batch.js --images 0 <slug>`** (이미지는 이미 생성됨 → 발행만).
> - Flux 호출이 실패하면(예: 404 `No adapter found`, 429 rate limit) **같은 Flux 경로로 재시도**한다. Gemini로 우회하지 않는다.

> 🖼️ **이미지 2장 규칙 (필수 · 썸네일 ≠ 본문)** — 글마다 **서로 다른 일러스트 2장**을 생성·사용한다. 스타일 기준은 https://babygrade.kr/maternity-body-pillow-2026/ (파스텔 플랫 벡터 일러스트, 텍스트 없음).
> - **`-1.png` = 썸네일/OG 전용** (카드·검색 미리보기). frontmatter `heroImage`에만 사용. → 단일 제품 실루엣 "커버" 컷.
> - **`-2.png` = 본문 `<figure>` 전용**. 썸네일과 **다른 구도**(예: 3제품 비교 일러스트). 본문에는 절대 `-1.png`를 넣지 말 것.
> - 즉 `heroImage: /images/{slug}-1.png` + 본문 `<img src="/images/{slug}-2.png">`. `publish-batch.js` 기본값이 `--images 2`이므로 그대로 두 장 생성된다(`--images 1`은 금지 — 썸네일=본문 중복 발생).
>
> ⚠️ **Flux 생성 실패 시 처리** — Gemini를 쓰지 않으므로 프롬프트 합성 장애는 애초에 발생하지 않는다. Replicate Flux 호출 자체가 실패할 때만 대응한다.
> - **404 `No adapter found for model`** — 일시 오류. 같은 호출을 **1회 재시도**하면 대개 통과한다(2026-07-28 실증).
> - **429 rate limit** — 계정 잔액 <$5면 분당 6 prediction으로 제한. 슬러그별 **순차 호출**로 낮추거나 잔액 충전.
> - 단일 이미지만 재생성할 때는 인덱스를 직접 지정한다: `generateImage(buildImagePrompt(영문_묘사), "../affiliate-site/public/images/<slug>-2.png")`.
> - **rsvg 로컬 SVG 폴백은 Replicate가 완전히 불가일 때만 최후로** 쓰고, 회복 후 Flux로 교체한다. **어떤 경우에도 Gemini로 우회하지 않는다.**

**Google 톤 시각:**
- 비교 차트 (막대/원형/표)
- 등급/티어 시스템 (S/A/B/C 색상 구분)
- 데이터 그래프 (가격 vs 효능)
- 의사결정 트리 (어떤 상황에 어떤 제품)

### 4️⃣ 빌드 검증

```bash
cd ../affiliate-site
npm run build
```

### 5️⃣ 자동 발행 (한 줄로 끝)

```bash
cd content-pipeline
node publish.js [slug]
```

자동 실행:
1. `npm run build` — 빌드 검증
2. `git push` — GitHub로 푸시
3. **Cloudflare 배포 대기** — 새 URL이 200 떨어질 때까지 폴링
4. **IndexNow 핑** — Bing/Yandex에 즉시 통보 (홈/sitemap/llms.txt/새 글)
5. **llms.txt 자동 갱신** — Astro 빌드 시 `/llms.txt`가 모든 글 포함하도록 동적 생성
6. **GSC sitemap 자동 재크롤** — sitemap-index.xml은 Google이 1~7일 주기로 자동 재방문

#### ⚡ 여러 글 한 번에 발행 (3개/일 정상 페이스 권장)

`publish.js`를 글마다 N번 돌리면 **이미지가 직렬 생성 + 배포·폴링이 N번** 반복돼 느리다.
3개 이상 발행 시에는 일괄 스크립트를 쓴다 — **이미지 병렬 생성 + 빌드/배포/폴링 1회 + IndexNow 일괄 핑**.

```bash
# 본문 작성(.md)까지 끝낸 슬러그들을 한 번에:
node publish-batch.js slug1 slug2 slug3
node publish-batch.js --images 1 slug1 slug2 slug3   # 본문에 인포그래픽 1장만 쓸 때(6장→3장)
node publish-batch.js --images 0 slug1 slug2 slug3   # 이미지 이미 생성됨 → 발행만
node publish-batch.js --concurrency 6 ...            # 이미지 동시 생성 수(기본 4)
```

> 참고: 본문에 `-2.png`(둘째 인포그래픽)를 안 넣을 거면 `--images 1`로 절반 시간 절약.
> 기본 2장은 기존 글 관습(본문 figure 2개)과 맞춘 값이다.

---

## 🌐 토픽 클러스터 전략 (카니발라이제이션 방지)

### Hub & Spoke 모델

```
[허브] 강아지 사료 완벽 가이드 (메인키워드: "강아지 사료")
   │
   ├─[스포크] 강아지 알러지 사료 (mainKeyword: "강아지 알러지 사료")
   ├─[스포크] 강아지 다이어트 사료 (mainKeyword: "강아지 다이어트 사료")
   └─[스포크] 강아지 노령 사료 (mainKeyword: "강아지 노령 사료")
```

### 절대 규칙

1. **각 글의 `mainKeyword`는 유일해야 함** — `new-article.js`가 자동 검사
2. **유사도 50%+ 시 경고**, 66%+ 시 경고, 100% 시 발행 차단
3. **롱테일 우선** — 좁은 키워드(스포크) 먼저, 허브는 나중에 (10~20개 스포크 쌓이면)
4. **검색 의도 분리해도 OK**:
   - "강아지 사료 추천" (comparison) ↔ "강아지 사료 디시" (review) ↔ "강아지 사료 등급표" (comparison)
   - 의도가 다르면 카니발라이제이션 X (단, 실제 검색 결과가 다른지 확인 권장)
5. **내부 링크 의무** — 새 글 발행 시 같은 카테고리 기존 글 2~3개 자동 연결

### 발행 페이스 권장

| 시기 | 일 발행 수 | 이유 |
|---|---|---|
| 1주차 | 1개 | Sandboxing 회피 (신생 사이트 신뢰 형성) |
| 2~3주차 | 2개 | 점진 증가 |
| 4주차+ | 3개 | 정상 페이스 |
| 50개 글 도달 후 | 3~5개 | 권위 형성, 안전 |

> ⚠️ 위 페이스 표는 **색인이 정상일 때** 기준. 색인률이 무너지면 아래 동결 규칙이 우선한다.

### 📌 발행 케이던스 = **1일 1회 정기 발행** (2026-07-28 확정)

> **상한이 아니라 목표치다.** 6/18~7/27은 "하루 **최대** 1개"(색인 회복용 상한)로 운영했고, 그 결과 7/7~7/15(9일)·7/17~7/27(11일) 두 차례 장기 공백이 생겼다. 공백은 색인에 도움이 되지 않고 **시즌 리드타임만 까먹는다**(여름 키워드 한 시즌 통째로 유실).
> → 2026-07-28부터 **매일 1개씩 꾸준히** 발행한다. 하루 1개는 이제 **지켜야 할 하한이자 상한**이다.

**발행 규칙 (반드시 준수)**:
1. **1일 1회.** 매일 1개 발행이 기본. **2개 이상 몰아치기 금지**(3개/일 양산이 색인 붕괴 원인), **며칠씩 건너뛰기도 금지**(공백은 시즌 리드타임 손실).
2. **차별화 필수** — 공통 보일러플레이트 최소화, 글마다 고유 데이터(실측 가격표·고유 FAQ·원본 비교). 똑같은 등급표 템플릿 복붙 금지.
3. **네이버 수요 키워드 우선** — 한국 육아 검색량 큰 것부터 (네이버가 메인 채널).
4. **내부링크는 `/slug/`** (끝 슬래시 필수 — 리다이렉트 방지).

**밀렸을 때 처리 (중요)**:
- 하루 빠뜨렸다고 **다음 날 2개로 벌충하지 않는다.** 밀린 주제는 그대로 하루씩 뒤로 밀고, 시즌성이 죽은 주제(피크월 경과)는 **그 자리에서 폐기하고 다음 슬레이트로 교체**한다.
- 3일 이상 공백이 생기면 다음 발행 전에 **데이터 재측정부터** 한다(`seasonality.sh` + 네이버) — 공백 동안 시즌 우선순위가 바뀌었을 수 있다.

**병행 (계속)**:
- GSC 우선순위 색인 요청 + 주 1회 `node gsc.js index babygrade.kr` (토큰 만료 시 `node get-gsc-token.js`)
- 외부 백링크 천천히 1~2개/주 (티스토리·핀터레스트·homegrade 본문)
- 네이버 서치어드바이저 sitemap 재제출

**금지 (재발 방지)**:
- 하루 2개 이상 발행 · 밀린 분량 몰아서 벌충
- 동일 구조 템플릿 양산 (차별화 없는 등급표 복붙)
- 같은 URL 색인 요청 도배

> 배경(2026-06-18): 6/16 전면 동결 → 6/18 완화. 네이버 서치어드바이저 확인 결과 **네이버는 babygrade를 색인 22개로 잘 받아주고 있었고**(구글 4 vs 네이버 22), 한국 육아 검색은 네이버가 메인 채널. 구글을 망친 건 *발행 자체가 아니라 "속도(한 달 87개) + 동질성(템플릿)"*. 따라서 **느린 차별화 발행은 네이버에 이득, 구글에 무해** — 이 판단이 1일 1회 정기 발행의 근거다. 구글 색인은 1~3개월 점진 회복(부분), 네이버는 즉시 성장 채널로 활용.

---

## 📋 카테고리 가이드 (Google 트래픽 기준)

Google 자동완성에서 발견한 **돈이 되는 의도 패턴**:

| 카테고리 | 강한 검색 의도 | 콘텐츠 우선순위 |
|---|---|---|
| **사료** | 등급표, 디시 후기, 추천 디시 | 등급 평가 + 디시 후기 종합 |
| **간식** | 추천, 만들기, 디시 | 비교 + DIY 가이드 |
| **영양제** | **논란 / oem / 디시 / 광고** ⭐ | **신뢰 검증** + 등급 비교 |
| **알러지** | 검사 비용, 검사 키트, 디시 | 가성비 검사 옵션 비교 |
| **다이어트** | 사료/식단, 채소, 디시 | 비교 + 메뉴 가이드 |
| **슬개골** | 탈구 증상, 수술 비용, 보호대 | 단계별 진행 가이드 |
| **피부** | 피부병 종류/사진/딱지 | 증상별 분류 가이드 |

⭐ **"강아지 영양제"는 논란 키워드가 강해서 신뢰 검증 콘텐츠 1순위**

---

## 🚦 품질 게이트

발행 전 자동 검증 (`seo-validator.js`).

추가로 Google 톤 체크:
- [ ] 감성어 사용 최소화 ("따뜻한" "사랑스러운" 금지)
- [ ] 데이터/수치 명시 (모호한 "많이" "조금" 금지)
- [ ] 비교 표 1개 이상
- [ ] **출처 1~2개 본문 인용** (단순 frontmatter 아님)

---

## 🔁 사이클타임

| 단계 | 시간 |
|---|---|
| 1. 데이터 수집 + 의도 분류 | 10초 |
| 2. 본문 작성 (Google 톤) | 5~7분 |
| 3. 인포그래픽 생성 | 30초 |
| 4. 빌드 + push | 1분 |
| **총** | **약 7~9분/글** |

---

## 📁 파일 위치

```
content-pipeline/
├── new-article.js          # 1단계: 데이터 + 스켈레톤
├── google-keywords.js      # ⭐ Google 자동완성 + 의도 분류
├── generate-images.js      # 3단계: 인포그래픽
├── coupang.js              # 쿠팡 API
└── naver-keywords.js       # (deprecated, Google으로 대체)
```
